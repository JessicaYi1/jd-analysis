
# PRD：AI 产品经理实习 JD × 简历匹配分析系统

## 1. 项目背景

当前已有一个 JD 智能分析工具，面向想转行产品经理，尤其是 AI 产品经理实习的用户。

现有功能包括：

1. 通过浏览器书签脚本 `bookmarklet.js` 在招聘网站采集岗位 JD。
2. 后端 `backend.py / FastAPI` 提供 `POST /add_job` 接收 JD。
3. 后端调用 LLM，对 JD 进行结构化分析，包括：

   * 岗位分类：AI 产品经理、策略产品经理、中后台产品经理、通用 PM
   * 硬技能提取
   * 隐藏要求识别
   * 面试题生成
4. 前端 `dashboard.py / Streamlit` 展示已分析岗位列表。
5. 支持多选岗位后调用 `POST /analyze_jobs`，批量生成差距分析和学习路径。

本次需求希望参考 Resume Matcher 类产品的思路，在现有基础上新增：

> 简历解析、JD 与简历匹配度评分、关键词覆盖分析、简历优化建议、投递话术生成。

目标是将项目从：

> JD 智能分析工具

升级为：

> 面向 AI 产品经理实习求职场景的岗位采集、能力诊断、简历优化与求职行动规划系统。

---

# 2. 产品目标

## 2.1 核心目标

帮助用户完成以下闭环：

```text
采集目标岗位 JD
→ 结构化分析岗位要求
→ 解析用户简历
→ 计算 JD 与简历匹配度
→ 输出能力缺口和简历优化建议
→ 生成投递话术
→ 支持用户反馈 Badcase
```

## 2.2 用户对象

目标用户是：

1. 想转行 AI 产品经理实习的学生或应届生。
2. 有传统产品、运营、数据、技术或项目经历，但不知道如何包装成 AI PM 相关经历。
3. 不清楚目标岗位真正看重哪些能力。
4. 需要判断哪些岗位值得投、简历如何改、短期应该补什么能力。

## 2.3 MVP 原则

本版本优先保证 Demo 链路完整，不追求复杂 UI 和高级算法。

MVP 链路如下：

```text
用户粘贴简历
→ 系统解析简历画像
→ 用户选择一个已采集 JD
→ 系统生成匹配度评分
→ 展示优势、缺口、风险
→ 生成简历项目经历改写建议
→ 生成 Boss 直聘 / 邮件 / 内推话术
```

---

# 3. 功能范围

## 3.1 本期必须实现 P0

### P0-1：简历文本粘贴与结构化解析

用户可以在 Streamlit 页面中粘贴自己的简历文本，点击按钮后，后端调用 LLM 解析简历，生成结构化用户画像。

---

### P0-2：单个 JD 与简历匹配度评分

用户可以选择一个岗位，点击“生成匹配度分析”，系统基于 JD 分析结果和简历画像生成匹配度报告。

---

### P0-3：关键词覆盖分析

系统对比 JD 核心关键词和简历关键词，输出：

1. JD 核心关键词
2. 简历已覆盖关键词
3. 简历缺失关键词
4. 建议补充关键词
5. 不建议强行添加的关键词

---

### P0-4：简历项目经历改写建议

系统基于目标 JD 和用户简历，生成适合该岗位的项目经历改写建议。

默认重点优化用户的 AI 项目：

```text
JD 智能分析与简历匹配系统
```

---

### P0-5：投递话术生成

系统基于 JD、简历画像和匹配报告，生成：

1. Boss 直聘开场白
2. 邮件正文
3. 内推私信

---

## 3.2 P1 后续功能

### P1-1：多 JD 批量简历优化

复用现有多选岗位能力，生成多个岗位的共同要求、简历优化方向和岗位优先级排序。

---

### P1-2：Badcase 反馈机制

用户可以对 JD 分析、匹配评分、简历改写建议进行反馈。

---

### P1-3：岗位类型自动切换评分权重

系统根据岗位分类，自动切换不同评分模型。

---

## 3.3 暂不实现

以下功能本期不做：

1. PDF / DOCX 简历上传解析。
2. 简历 PDF 导出。
3. 多语言 UI。
4. 登录注册。
5. 多用户权限。
6. 向量数据库。
7. Embedding 语义相似度计算。

本期先使用：

```text
LLM 结构化抽取 + 系统预设权重 + 代码计算总分 + LLM 生成解释
```

---

# 4. 核心产品流程

## 4.1 完整用户流程

```text
1. 用户使用 bookmarklet.js 在招聘网站采集 JD
2. JD 被发送到 POST /add_job
3. 后端调用 LLM 分析 JD，并保存岗位分析结果
4. 用户打开 Streamlit 看板
5. 用户进入“我的简历”模块
6. 用户粘贴简历文本
7. 点击“解析简历”
8. 后端调用 POST /parse_resume
9. 系统生成 resume_profile
10. 用户回到岗位列表，选择某个 JD
11. 点击“生成匹配度分析”
12. 后端调用 POST /match_resume_job
13. 系统输出匹配度评分、优势、缺口、风险、优化建议
14. 用户点击“生成简历改写建议”
15. 后端调用 POST /generate_resume_rewrite
16. 系统生成可直接复制的项目经历 bullet points
17. 用户点击“生成投递话术”
18. 后端调用 POST /generate_application_message
19. 系统生成 Boss 直聘、邮件、内推私信三种话术
```

---

# 5. 页面设计

## 5.1 Streamlit 页面结构

建议在现有 `dashboard.py` 中新增以下模块：

```text
1. 岗位列表 / JD 看板
2. 我的简历 Profile
3. 单岗位匹配分析
4. 简历优化建议
5. 投递话术生成
6. 多岗位批量分析，已有功能可增强
```

如果当前项目结构较简单，也可以不做复杂导航，只在现有页面中按区域展示。

---

## 5.2 “我的简历 Profile”页面

### 页面元素

```text
标题：我的简历 Profile

输入区：
- st.text_area("请粘贴你的简历文本")

按钮：
- st.button("解析简历")

展示区：
- 教育背景
- 目标岗位
- 实习经历
- 项目经历
- 硬技能
- AI 技能
- 产品技能
- 数据技能
- 当前短板
```

### 页面交互

用户点击“解析简历”后：

1. 前端调用 `POST /parse_resume`
2. 后端返回 `resume_profile`
3. 前端展示结构化结果
4. 同时保存到本地文件或数据库

---

## 5.3 岗位详情 / 岗位列表页面

在每个岗位卡片中新增按钮：

```text
生成匹配度分析
```

点击后：

1. 读取当前岗位的 `job_analysis`
2. 读取当前用户的 `resume_profile`
3. 调用 `POST /match_resume_job`
4. 展示匹配度报告

---

## 5.4 匹配度报告展示

建议展示方式：

```text
总匹配度：72 / 100
投递建议：建议投递，但需要优化 AI 产品能力表达
置信度：中
```

然后分区展示：

```text
一、评分明细
二、已匹配优势
三、缺失能力
四、风险提示
五、关键词覆盖分析
六、下一步行动建议
```

评分明细可以使用：

```python
st.metric()
st.progress()
st.table()
```

---

# 6. 数据结构设计

如果当前项目使用 JSON 文件存储，可以新增以下 JSON 文件：

```text
data/resume_profile.json
data/match_reports.json
data/feedback_logs.json
```

如果当前使用 SQLite，也可以创建对应表。

---

## 6.1 Job 数据结构

沿用现有结构，建议保证包含以下字段：

```json
{
  "id": "job_001",
  "title": "AI产品经理实习生",
  "company": "某公司",
  "url": "https://example.com/job/123",
  "jd_text": "岗位原始JD文本",
  "category": "AI产品经理",
  "job_analysis": {
    "hard_skills": ["SQL", "Python", "数据分析"],
    "ai_skills": ["LLM", "Prompt", "RAG"],
    "product_skills": ["需求分析", "PRD", "用户调研"],
    "business_scenarios": ["AIGC", "知识库", "智能客服"],
    "hidden_requirements": ["自驱力", "跨团队沟通", "快速学习"],
    "interview_questions": []
  },
  "created_at": "2026-05-25T00:00:00"
}
```

---

## 6.2 Resume Profile 数据结构

新增：

```json
{
  "id": "resume_001",
  "raw_text": "用户粘贴的原始简历文本",
  "target_role": "AI产品经理实习",
  "education": {
    "school": "",
    "degree": "",
    "major": "",
    "graduation_time": ""
  },
  "internship_experience": [
    {
      "company": "芒果TV",
      "role": "产品经理实习生",
      "time": "2025.09 - 2025.12",
      "summary": "参与搜索改版项目",
      "keywords": ["搜索", "内容平台", "需求分析", "用户体验"]
    }
  ],
  "projects": [
    {
      "name": "JD智能分析与简历匹配系统",
      "summary": "通过采集招聘JD并调用LLM进行岗位分类、技能提取和差距分析",
      "skills": ["FastAPI", "Streamlit", "LLM", "Prompt", "岗位分类", "差距分析"],
      "role": "独立设计并开发"
    }
  ],
  "hard_skills": ["Python", "SQL", "FastAPI", "Streamlit"],
  "ai_skills": ["LLM调用", "Prompt设计", "AI工具使用"],
  "product_skills": ["需求分析", "PRD", "竞品分析", "用户调研"],
  "data_skills": ["数据分析", "指标拆解"],
  "weaknesses": ["RAG", "Agent", "模型评估", "Badcase分析"],
  "created_at": "2026-05-25T00:00:00",
  "updated_at": "2026-05-25T00:00:00"
}
```

---

## 6.3 Match Report 数据结构

新增：

```json
{
  "id": "match_001",
  "job_id": "job_001",
  "resume_id": "resume_001",
  "job_category": "AI产品经理",
  "weight_config": {
    "ai_product_ability": 0.30,
    "project_relevance": 0.25,
    "product_fundamentals": 0.20,
    "data_analysis": 0.15,
    "keyword_coverage": 0.10
  },
  "score_breakdown": {
    "ai_product_ability": {
      "score": 62,
      "weighted_score": 18.6,
      "evidence": [
        "简历中有 LLM 调用和 Streamlit 看板项目",
        "项目体现了 AI 工具使用和岗位分析场景"
      ],
      "missing": [
        "RAG",
        "Agent",
        "模型效果评估",
        "Badcase分析"
      ]
    },
    "project_relevance": {
      "score": 78,
      "weighted_score": 19.5,
      "evidence": [
        "JD 智能分析项目与 AI 产品经理求职场景相关",
        "项目包含需求采集、LLM 分析和看板展示"
      ],
      "missing": [
        "缺少真实用户反馈数据",
        "缺少量化指标"
      ]
    },
    "product_fundamentals": {
      "score": 75,
      "weighted_score": 15,
      "evidence": [
        "有产品经理实习经历",
        "简历中体现需求分析和产品设计"
      ],
      "missing": [
        "PRD 产出描述不够明确",
        "缺少用户调研过程"
      ]
    },
    "data_analysis": {
      "score": 65,
      "weighted_score": 9.75,
      "evidence": [
        "项目中包含看板展示",
        "有一定数据分析意识"
      ],
      "missing": [
        "SQL 使用不明显",
        "缺少指标体系和转化分析"
      ]
    },
    "keyword_coverage": {
      "score": 60,
      "weighted_score": 6,
      "evidence": [
        "覆盖 LLM、产品分析、数据看板等关键词"
      ],
      "missing": [
        "RAG",
        "Prompt优化",
        "模型评估"
      ]
    }
  },
  "overall_score": 69,
  "recommendation": "建议投递，但需要优化 AI 产品能力表达",
  "confidence": "medium",
  "matched_points": [
    "有产品经理实习经历",
    "有独立开发 AI 工具的项目经历",
    "具备 Python + FastAPI + Streamlit 的技术实现能力"
  ],
  "missing_points": [
    "缺少 RAG、Agent、模型评估相关表达",
    "项目缺少量化指标",
    "AI 产品方法论表达不足"
  ],
  "risks": [
    "如果 JD 明确要求模型评估经验，当前简历竞争力偏弱",
    "如果岗位偏算法平台，当前经历可能不够深入"
  ],
  "action_suggestions": [
    "在项目经历中补充 Prompt 设计和 Badcase 迭代过程",
    "补充 RAG 或 Agent 相关小 Demo",
    "将项目定位从 JD 分析工具升级为 AI 求职决策系统"
  ],
  "keyword_analysis": {
    "jd_core_keywords": [
      {
        "keyword": "LLM",
        "importance": "high"
      },
      {
        "keyword": "Prompt",
        "importance": "high"
      }
    ],
    "resume_covered_keywords": ["LLM", "Python", "Streamlit", "需求分析"],
    "missing_keywords": [
      {
        "keyword": "RAG",
        "importance": "high",
        "reason": "JD 中出现，但简历未体现相关项目或能力"
      }
    ],
    "recommended_keywords_to_add": ["Prompt优化", "Badcase分析", "模型评估"],
    "keywords_not_to_force": ["算法训练", "模型微调"]
  },
  "score_explanation": "该分数基于 JD 显性要求、简历内容和 AI 产品经理岗位能力模型综合计算，仅用于辅助判断岗位适配度，不代表招聘方真实筛选结果。",
  "created_at": "2026-05-25T00:00:00"
}
```

---

# 7. 评分规则设计

## 7.1 核心原则

不要让 LLM 直接“拍脑袋”输出总分。

正确做法是：

```text
LLM 负责结构化分析和维度打分
系统代码负责按权重计算总分
LLM 负责解释分数、优势、缺口和建议
```

---

## 7.2 MVP 默认评分模型

MVP 阶段默认面向 AI 产品经理实习岗位，采用以下权重：

| 维度      |  权重 | 说明                                        |
| ------- | --: | ----------------------------------------- |
| AI 产品能力 | 30% | LLM、Prompt、RAG、Agent、模型评估、Badcase、AI 应用落地 |
| 项目经历相关性 | 25% | 是否有能支撑目标岗位的项目或实习经历                        |
| 产品基本功   | 20% | 需求分析、PRD、用户调研、竞品分析、原型、沟通协作                |
| 数据分析能力  | 15% | SQL、Python、指标体系、A/B 测试、数据看板               |
| 关键词覆盖   | 10% | 简历是否覆盖 JD 高频核心关键词                         |

---

## 7.3 总分计算方式

后端代码中计算：

```python
overall_score = (
    ai_product_ability_score * 0.30 +
    project_relevance_score * 0.25 +
    product_fundamentals_score * 0.20 +
    data_analysis_score * 0.15 +
    keyword_coverage_score * 0.10
)
```

最终四舍五入为整数。

---

## 7.4 各维度评分标准

### AI 产品能力

| 分数区间   | 判断标准                                            |
| ------ | ----------------------------------------------- |
| 80-100 | 有完整 AI 项目，能体现 AI 流程设计、Prompt 迭代、模型评估、Badcase 分析 |
| 60-79  | 有 LLM 调用或 AI 工具项目，但评估闭环不足                       |
| 40-59  | 只提到 AI 工具使用，缺少产品化理解                             |
| 0-39   | 简历几乎没有 AI 相关内容                                  |

---

### 项目经历相关性

| 分数区间   | 判断标准                   |
| ------ | ---------------------- |
| 80-100 | 项目与 JD 场景高度相关，且能体现个人主导 |
| 60-79  | 项目能力相关，但业务场景不完全匹配      |
| 40-59  | 有项目，但与目标岗位关联较弱         |
| 0-39   | 项目经历不足，无法支撑岗位要求        |

---

### 产品基本功

考察是否体现：

1. 需求分析
2. 用户调研
3. PRD
4. 原型设计
5. 竞品分析
6. 项目推进
7. 跨团队沟通
8. 产品指标意识

---

### 数据分析能力

考察是否体现：

1. SQL
2. Python
3. 指标体系
4. 数据看板
5. A/B 测试
6. 转化分析
7. 归因分析

---

### 关键词覆盖

MVP 可以让 LLM 输出关键词覆盖分析，同时由代码计算一个简单覆盖率：

```python
keyword_coverage_score = len(resume_covered_keywords) / len(jd_core_keywords) * 100
```

需要注意：

1. 只计算高重要性和中重要性的 JD 核心关键词。
2. 不要鼓励用户强行堆关键词。
3. 输出 `keywords_not_to_force`，避免简历造假或过度包装。

---

## 7.5 投递建议规则

根据总分输出建议：

|     总分 | 投递建议                |
| -----: | ------------------- |
| 80-100 | 高度匹配，建议优先投递         |
|  65-79 | 基本匹配，建议投递，但需要优化简历表达 |
|  50-64 | 谨慎投递，建议先补关键能力或调整简历  |
|   0-49 | 暂不建议投递，当前缺口较大       |

---

## 7.6 置信度规则

新增 `confidence` 字段：

| 置信度    | 判断条件                   |
| ------ | ---------------------- |
| high   | JD 信息完整，简历信息完整，岗位分类明确  |
| medium | JD 或简历信息较完整，但部分能力需要推断  |
| low    | JD 信息过短、简历内容过少、岗位类别不明确 |

---

# 8. 后端接口设计

## 8.1 POST /parse_resume

### 功能

解析用户粘贴的简历文本，生成结构化 `resume_profile`。

### Request

```json
{
  "resume_text": "用户粘贴的简历文本"
}
```

### Response

```json
{
  "success": true,
  "resume_profile": {
    "id": "resume_001",
    "target_role": "AI产品经理实习",
    "education": {},
    "internship_experience": [],
    "projects": [],
    "hard_skills": [],
    "ai_skills": [],
    "product_skills": [],
    "data_skills": [],
    "weaknesses": []
  }
}
```

---

## 8.2 POST /match_resume_job

### 功能

对单个 JD 和用户简历进行匹配度分析。

### Request

```json
{
  "job_id": "job_001",
  "resume_id": "resume_001"
}
```

如果当前项目没有多简历管理，也可以直接传：

```json
{
  "job": {},
  "resume_profile": {}
}
```

### Response

```json
{
  "success": true,
  "match_report": {
    "id": "match_001",
    "job_id": "job_001",
    "resume_id": "resume_001",
    "overall_score": 69,
    "recommendation": "建议投递，但需要优化 AI 产品能力表达",
    "confidence": "medium",
    "score_breakdown": {},
    "matched_points": [],
    "missing_points": [],
    "risks": [],
    "action_suggestions": [],
    "keyword_analysis": {}
  }
}
```

---

## 8.3 POST /generate_resume_rewrite

### 功能

基于目标 JD，生成简历项目经历改写建议。

### Request

```json
{
  "job_id": "job_001",
  "resume_id": "resume_001",
  "project_name": "JD智能分析与简历匹配系统"
}
```

### Response

```json
{
  "success": true,
  "rewrite_report": {
    "rewrite_strategy": "突出 AI 产品能力、LLM 分析链路、岗位分类、匹配评分和求职决策闭环",
    "optimized_bullets": [
      "独立设计并开发面向 AI 产品经理实习求职场景的 JD 智能分析与简历匹配系统，支持通过浏览器书签脚本采集招聘 JD，并调用 LLM 自动完成岗位分类、硬技能提取、隐藏要求识别与面试题生成。",
      "参考 Resume Matcher 的匹配评分思路，设计 JD-简历匹配度评分、关键词覆盖分析和能力差距诊断模块，帮助转行用户判断岗位适配度并制定简历优化方向。",
      "基于 FastAPI + Streamlit 搭建分析看板，支持岗位列表展示、多岗位对比、学习路径生成和投递话术输出，形成岗位采集、智能分析、简历优化、求职行动的闭环。"
    ],
    "keywords_to_include": [
      "LLM",
      "Prompt设计",
      "岗位分类",
      "匹配度评分",
      "关键词覆盖",
      "Badcase反馈",
      "AI求职决策"
    ],
    "metrics_to_collect": [
      "累计采集并分析 JD 数量",
      "单条 JD 分析耗时",
      "岗位分类准确率人工校验结果",
      "用户反馈中有用建议占比"
    ],
    "warnings": [
      "不要夸大模型训练或算法开发经验",
      "如果没有真实用户数据，不要写成大规模上线产品"
    ]
  }
}
```

---

## 8.4 POST /generate_application_message

### 功能

生成投递话术。

### Request

```json
{
  "job_id": "job_001",
  "resume_id": "resume_001",
  "match_report_id": "match_001"
}
```

### Response

```json
{
  "success": true,
  "messages": {
    "boss_zhipin_opening": "您好，我正在寻找 AI 产品经理实习机会。此前有产品经理实习经历，并独立开发过一个面向 AI PM 求职场景的 JD 智能分析与简历匹配系统，涉及 JD 采集、LLM 岗位分类、技能提取、匹配度评分和差距分析看板。看到贵岗位强调 AI 应用落地和数据分析能力，我认为与我的经历比较匹配，希望有机会进一步沟通。",
    "email_body": "您好，我是 XXX，正在应聘贵司 AI 产品经理实习岗位……",
    "referral_message": "您好，看到贵团队正在招聘 AI 产品经理实习生……"
  }
}
```

---

## 8.5 POST /submit_feedback

### 功能

记录用户对分析结果的反馈。

### Request

```json
{
  "target_type": "match_report",
  "target_id": "match_001",
  "feedback_type": "not_accurate",
  "comment": "RAG 不应该算这个岗位的核心缺口"
}
```

### Response

```json
{
  "success": true,
  "message": "反馈已记录"
}
```

---

# 9. Pydantic Schema 建议

在 `backend.py` 或单独的 `schemas.py` 中新增：

```python
from pydantic import BaseModel
from typing import List, Dict, Optional, Any

class ParseResumeRequest(BaseModel):
    resume_text: str

class MatchResumeJobRequest(BaseModel):
    job_id: str
    resume_id: Optional[str] = None
    resume_profile: Optional[Dict[str, Any]] = None

class GenerateResumeRewriteRequest(BaseModel):
    job_id: str
    resume_id: Optional[str] = None
    project_name: Optional[str] = "JD智能分析与简历匹配系统"

class GenerateApplicationMessageRequest(BaseModel):
    job_id: str
    resume_id: Optional[str] = None
    match_report_id: Optional[str] = None

class SubmitFeedbackRequest(BaseModel):
    target_type: str
    target_id: str
    feedback_type: str
    comment: Optional[str] = ""
```

---

# 10. Prompt 设计要求

建议将所有 Prompt 放到：

```text
prompts.py
```

或：

```text
prompts/
  parse_resume.txt
  match_resume_job.txt
  generate_resume_rewrite.txt
  generate_application_message.txt
```

---

## 10.1 简历解析 Prompt

```text
你是一个 AI 产品经理求职顾问，请将用户提供的简历文本解析成结构化 JSON。

要求：
1. 不要编造简历中没有的信息。
2. 如果某项信息没有出现，请用空字符串或空数组。
3. 请重点识别与产品经理、AI 产品经理、数据分析、项目经历相关的内容。
4. 输出必须是合法 JSON，不要输出 Markdown，不要输出解释文字。

请按照以下 JSON 格式输出：

{
  "target_role": "",
  "education": {
    "school": "",
    "degree": "",
    "major": "",
    "graduation_time": ""
  },
  "internship_experience": [
    {
      "company": "",
      "role": "",
      "time": "",
      "summary": "",
      "keywords": []
    }
  ],
  "projects": [
    {
      "name": "",
      "summary": "",
      "skills": [],
      "role": ""
    }
  ],
  "hard_skills": [],
  "ai_skills": [],
  "product_skills": [],
  "data_skills": [],
  "weaknesses": []
}

用户简历文本如下：
{{resume_text}}
```

---

## 10.2 匹配度分析 Prompt

```text
你是一个 AI 产品经理求职匹配分析助手。你的任务是基于岗位 JD 分析结果和用户简历画像，逐项判断用户与岗位的匹配程度。

重要规则：
1. 不要直接拍脑袋给总分。
2. 你需要分别给以下 5 个维度打分，每项 0-100：
   - ai_product_ability
   - project_relevance
   - product_fundamentals
   - data_analysis
   - keyword_coverage
3. 每个分数必须给出 evidence 和 missing。
4. 不要编造用户没有的经历。
5. 如果某项能力只是建议补充，请放到 missing 或 action_suggestions，不要当作已具备能力。
6. 输出必须是合法 JSON，不要输出 Markdown，不要输出解释文字。
7. overall_score 可以先留空，由代码根据权重计算。
8. 请判断 confidence：high / medium / low。

评分参考：
AI 产品能力：
- 80-100：有完整 AI 项目，体现 AI 流程、Prompt 迭代、模型评估、Badcase 分析
- 60-79：有 LLM 调用或 AI 工具项目，但评估闭环不足
- 40-59：只提到 AI 工具使用，缺少产品化理解
- 0-39：几乎没有 AI 相关内容

项目经历相关性：
- 80-100：项目与 JD 场景高度相关，且能体现个人主导
- 60-79：项目能力相关，但业务场景不完全匹配
- 40-59：有项目，但与岗位关联较弱
- 0-39：项目经历不足

产品基本功：
关注需求分析、PRD、原型、用户调研、竞品分析、项目推进、沟通协作。

数据分析能力：
关注 SQL、Python、指标体系、A/B 测试、数据看板、转化分析。

关键词覆盖：
对比 JD 核心关键词和简历已覆盖关键词。

请按照以下 JSON 输出：

{
  "job_category": "",
  "score_breakdown": {
    "ai_product_ability": {
      "score": 0,
      "evidence": [],
      "missing": []
    },
    "project_relevance": {
      "score": 0,
      "evidence": [],
      "missing": []
    },
    "product_fundamentals": {
      "score": 0,
      "evidence": [],
      "missing": []
    },
    "data_analysis": {
      "score": 0,
      "evidence": [],
      "missing": []
    },
    "keyword_coverage": {
      "score": 0,
      "evidence": [],
      "missing": []
    }
  },
  "matched_points": [],
  "missing_points": [],
  "risks": [],
  "action_suggestions": [],
  "keyword_analysis": {
    "jd_core_keywords": [
      {
        "keyword": "",
        "importance": "high"
      }
    ],
    "resume_covered_keywords": [],
    "missing_keywords": [
      {
        "keyword": "",
        "importance": "high",
        "reason": ""
      }
    ],
    "recommended_keywords_to_add": [],
    "keywords_not_to_force": []
  },
  "recommendation": "",
  "confidence": "medium",
  "score_explanation": ""
}

岗位 JD 分析结果：
{{job_analysis}}

用户简历画像：
{{resume_profile}}
```

---

## 10.3 简历项目经历改写 Prompt

```text
你是一个 AI 产品经理简历优化顾问。请基于目标岗位 JD 和用户简历，为用户生成针对该岗位的项目经历改写建议。

要求：
1. 不要编造用户没有做过的事情。
2. 可以帮助用户优化表达，但不能夸大为上线产品、真实商业指标或大规模用户，除非简历中明确提到。
3. 默认重点优化项目：{{project_name}}
4. 输出必须是合法 JSON，不要输出 Markdown，不要输出解释文字。
5. optimized_bullets 要适合直接放进中文简历，每条控制在 80-120 字左右。
6. 尽量突出 AI 产品能力、产品闭环、技术实现、用户价值和可量化指标。

输出格式：

{
  "rewrite_strategy": "",
  "optimized_bullets": [],
  "keywords_to_include": [],
  "metrics_to_collect": [],
  "warnings": []
}

岗位 JD 分析：
{{job_analysis}}

用户简历画像：
{{resume_profile}}

需要优化的项目名称：
{{project_name}}
```

---

## 10.4 投递话术 Prompt

```text
你是一个 AI 产品经理实习求职助手。请基于岗位 JD、用户简历和匹配报告，生成适合国内互联网实习投递场景的话术。

要求：
1. 语气自然，不要过度夸张。
2. 突出用户与岗位相关的经历。
3. 不要编造不存在的经历。
4. 输出三种话术：
   - Boss直聘开场白
   - 邮件正文
   - 内推私信
5. 输出必须是合法 JSON，不要输出 Markdown，不要输出解释文字。

输出格式：

{
  "boss_zhipin_opening": "",
  "email_body": "",
  "referral_message": ""
}

岗位 JD 分析：
{{job_analysis}}

用户简历画像：
{{resume_profile}}

匹配报告：
{{match_report}}
```

---

# 11. 后端实现要求

## 11.1 JSON 解析容错

所有 LLM 返回结果必须做 JSON 解析。

要求：

1. 优先直接 `json.loads()`
2. 如果失败，尝试提取第一个 `{}` 包裹的 JSON
3. 如果仍失败，返回错误信息给前端
4. 不要让整个服务崩溃

建议封装函数：

```python
def safe_parse_json(llm_output: str) -> dict:
    pass
```

---

## 11.2 总分计算函数

新增：

```python
def calculate_overall_score(score_breakdown: dict, weight_config: dict) -> int:
    pass
```

MVP 默认权重：

```python
DEFAULT_AI_PM_WEIGHTS = {
    "ai_product_ability": 0.30,
    "project_relevance": 0.25,
    "product_fundamentals": 0.20,
    "data_analysis": 0.15,
    "keyword_coverage": 0.10
}
```

计算后写回：

```python
match_report["overall_score"] = calculated_score
match_report["weight_config"] = DEFAULT_AI_PM_WEIGHTS
```

---

## 11.3 投递建议修正

可以在代码层根据总分修正 recommendation：

```python
def get_recommendation(score: int) -> str:
    if score >= 80:
        return "高度匹配，建议优先投递"
    elif score >= 65:
        return "基本匹配，建议投递，但需要优化简历表达"
    elif score >= 50:
        return "谨慎投递，建议先补关键能力或调整简历"
    else:
        return "暂不建议投递，当前缺口较大"
```

---

## 11.4 数据保存

如果使用 JSON 文件：

```text
data/resume_profile.json
data/match_reports.json
data/feedback_logs.json
```

每次新增记录时：

1. 读取已有 JSON
2. append 新记录
3. 写回文件
4. 注意文件不存在时自动创建

---

# 12. Streamlit 前端实现要求

## 12.1 简历模块

新增：

```python
st.header("我的简历 Profile")
resume_text = st.text_area("请粘贴你的简历文本", height=300)

if st.button("解析简历"):
    # call POST /parse_resume
    # save result to session_state
    # display result
```

建议保存：

```python
st.session_state["resume_profile"] = resume_profile
```

---

## 12.2 匹配度分析模块

在岗位列表中增加：

```python
if st.button(f"生成匹配度分析 - {job['title']}"):
    # call POST /match_resume_job
```

展示：

```python
st.metric("总匹配度", f"{overall_score}/100")
st.write("投递建议：", recommendation)
st.write("置信度：", confidence)
```

评分明细：

```python
for dimension, item in score_breakdown.items():
    st.subheader(dimension)
    st.progress(item["score"] / 100)
    st.write("证据：", item["evidence"])
    st.write("缺口：", item["missing"])
```

关键词分析：

```python
st.table(keyword_analysis)
```

---

## 12.3 简历改写模块

在匹配报告下方增加：

```python
if st.button("生成简历项目改写建议"):
    # call POST /generate_resume_rewrite
```

展示：

```python
st.subheader("推荐简历 Bullet Points")
for bullet in optimized_bullets:
    st.code(bullet)
```

---

## 12.4 投递话术模块

在匹配报告下方增加：

```python
if st.button("生成投递话术"):
    # call POST /generate_application_message
```

展示：

```python
st.subheader("Boss直聘开场白")
st.text_area("可复制", boss_zhipin_opening)

st.subheader("邮件正文")
st.text_area("可复制", email_body)

st.subheader("内推私信")
st.text_area("可复制", referral_message)
```

---

# 13. P1：多 JD 批量简历优化

在现有 `POST /analyze_jobs` 的基础上新增：

```text
POST /analyze_resume_for_jobs
```

## Request

```json
{
  "job_ids": ["job_001", "job_002", "job_003"],
  "resume_id": "resume_001"
}
```

## Response

```json
{
  "success": true,
  "batch_resume_analysis": {
    "common_requirements": [],
    "high_frequency_keywords": [],
    "resume_priority_suggestions": [],
    "project_packaging_suggestions": [],
    "job_priority_ranking": [],
    "seven_day_action_plan": []
  }
}
```

## 输出内容

```json
{
  "common_requirements": [
    "数据分析",
    "AI产品理解",
    "Prompt设计",
    "用户需求分析"
  ],
  "high_frequency_keywords": [
    "AIGC",
    "LLM",
    "SQL",
    "数据指标",
    "模型评估"
  ],
  "resume_priority_suggestions": [
    "优先强化 JD 智能分析与简历匹配系统项目",
    "补充 Prompt 设计和 Badcase 迭代表达",
    "增加数据指标和项目结果描述"
  ],
  "project_packaging_suggestions": [
    "将项目定位为 AI 求职决策系统，而不是单纯 JD 分析工具",
    "突出岗位采集、LLM 分析、匹配评分、学习路径生成的完整闭环"
  ],
  "job_priority_ranking": [
    {
      "job_id": "job_001",
      "priority": 1,
      "reason": "匹配度最高，建议优先投递"
    }
  ],
  "seven_day_action_plan": [
    {
      "day": "Day 1",
      "task": "补充简历中的 AI 项目描述",
      "output": "完成 3 条项目 bullet"
    }
  ]
}
```

---

# 14. P1：Badcase 反馈机制

## 14.1 前端反馈入口

在以下结果下方增加反馈按钮：

1. JD 分析结果
2. 匹配度报告
3. 简历改写建议
4. 投递话术

反馈选项：

```text
有用
不准确
需要修改
```

并提供可选输入框：

```text
请描述哪里不准确
```

---

## 14.2 feedback_logs 数据结构

```json
{
  "id": "feedback_001",
  "target_type": "match_report",
  "target_id": "match_001",
  "feedback_type": "not_accurate",
  "comment": "RAG 不应该算这个岗位的核心要求",
  "created_at": "2026-05-25T00:00:00"
}
```

---

# 15. 文件结构建议

在不大规模重构的前提下，建议新增：

```text
project/
  backend.py
  dashboard.py
  prompts.py
  data/
    jobs.json
    resume_profile.json
    match_reports.json
    feedback_logs.json
  utils/
    json_utils.py
    scoring.py
```

如果项目很小，也可以先不拆 `utils`，直接写在 `backend.py` 中。

---

# 16. 验收标准

## 16.1 P0 验收标准

### 简历解析

输入一段简历文本后，系统可以输出结构化简历画像，包括：

1. 教育背景
2. 实习经历
3. 项目经历
4. 硬技能
5. AI 技能
6. 产品技能
7. 数据技能
8. 当前短板

---

### 匹配度分析

选择任意一个已采集 JD 后，系统可以输出：

1. 总匹配度分数
2. 投递建议
3. 5 个维度评分
4. 每个维度的证据和缺口
5. 已匹配优势
6. 缺失能力
7. 风险提示
8. 下一步行动建议
9. 关键词覆盖分析

---

### 简历改写建议

系统可以生成至少 3 条可直接复制到简历中的项目经历 bullet points。

---

### 投递话术

系统可以生成：

1. Boss 直聘开场白
2. 邮件正文
3. 内推私信

---

### 数据保存

解析结果、匹配报告、反馈记录可以持久化保存。

---

# 17. Demo 展示路径

最终 Demo 按以下顺序展示：

```text
1. 打开招聘网站，使用 bookmarklet 采集一个 AI 产品经理实习 JD
2. 回到 Streamlit 看板，展示该 JD 已被自动分类为 AI 产品经理
3. 展示系统提取出的硬技能、隐藏要求和面试题
4. 进入“我的简历”，粘贴用户简历
5. 点击“解析简历”，展示结构化用户画像
6. 回到岗位详情，点击“生成匹配度分析”
7. 展示总分、分项评分、匹配优势和能力缺口
8. 展示关键词覆盖分析
9. 点击“生成简历项目改写建议”
10. 展示可复制的项目经历 bullet points
11. 点击“生成投递话术”
12. 展示 Boss 直聘、邮件、内推私信
13. 点击“不准确”反馈按钮，展示 Badcase 反馈机制
```

---

# 18. 开发优先级

建议编程助手按以下顺序实现：

```text
第一步：新增 prompts.py
第二步：新增 /parse_resume
第三步：在 Streamlit 增加“我的简历”模块
第四步：新增评分权重和 calculate_overall_score
第五步：新增 /match_resume_job
第六步：在岗位列表增加“生成匹配度分析”按钮
第七步：新增 /generate_resume_rewrite
第八步：新增 /generate_application_message
第九步：新增 feedback_logs 和 /submit_feedback
第十步：优化 UI 展示和 JSON 容错
```

---

# 19. 开发注意事项

1. 不要重构整个项目。
2. 保持现有 FastAPI + Streamlit 架构。
3. 所有 LLM 输出必须要求 JSON。
4. 所有 JSON 解析必须有 fallback。
5. 总分必须由代码计算，不要完全依赖 LLM。
6. 不要让系统声称可以预测真实录用概率。
7. 匹配分只用于辅助用户判断岗位适配度和简历优化优先级。
8. 所有建议都要基于用户简历和 JD，不要编造经历。
9. 简历改写可以优化表达，但不能夸大事实。
10. MVP 优先保证完整链路跑通，不追求复杂 UI。
